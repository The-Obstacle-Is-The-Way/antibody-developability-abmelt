.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# PyCharm
#  JetBrains specific template is maintained in a separate JetBrains.gitignore that can
#  be found at https://github.com/github/gitignore/blob/main/Global/JetBrains.gitignore
#  and can be added to the global gitignore or merged into this file.  For a more nuclear
#  option (not recommended) you can uncomment the following to ignore the entire idea folder.
#.idea/

# Abstra
# Abstra is an AI-powered process automation framework.
# Ignore directories containing user credentials, local state, and settings.
# Learn more at https://abstra.io/docs
.abstra/

# Visual Studio Code
#  Visual Studio Code specific template is maintained in a separate VisualStudioCode.gitignore
#  that can be found at https://github.com/github/gitignore/blob/main/Global/VisualStudioCode.gitignore
#  and can be added to the global gitignore or merged into this file. However, if you prefer,
#  you could uncomment the following to ignore the entire vscode folder
# .vscode/

# Ruff stuff:
.ruff_cache/

# PyPI configuration file
.pypirc

# Cursor
#  Cursor is an AI-powered code editor. `.cursorignore` specifies files/directories to
#  exclude from AI features like autocomplete and code analysis. Recommended for sensitive data
#  refer to https://docs.cursor.com/context/ignore-files
.cursorignore
.cursorindexingignore

# Marimo
marimo/_static/
marimo/_lsp/
__marimo__/

# ===========================================
# Editor backup/autosave files
# ===========================================
# Emacs
\#*\#
*~
.\#*

# Vim
*.swp
*.swo
*~

# ===========================================
# Simulation temp/intermediate files
# ===========================================
# GROMACS backup files (created when overwriting existing files)
#*#

# Intermediate trajectory processing files
md_whole_*.xtc
md_nopbcjump_*.xtc
md_*.xtc
md_*.gro
!md_final_*.xtc
!md_final_*.gro
!md_*.tpr

# Equilibration files
nvt_*.gro
nvt_*.xtc
nvt_*.tpr
nvt_*.cpt
nvt_*.edr
nvt_*.log
npt_*.gro
npt_*.xtc
npt_*.tpr
npt_*.cpt
npt_*.edr
npt_*.log

# System setup intermediates
box.gro
solv.gro
solv_ions.gro
em.gro
em.tpr
em.edr
em.log
em.trr
ions.tpr

# Covariance analysis intermediates
md_final_covar_*.xtc
covar_*.trr
covar_*.xvg
avg_covar*.pdb
covar_matrix_*.dat
covar.log
# Note: Other *.xvg files are required and should NOT be ignored

# PropKa output
*.pka

# MD run logs (except sconf logs which are kept)
md_*.log
md_*.edr
!sconf_*.log

# Checkpoint files (can be regenerated)
*.cpt

# Temporary MDP files created in work dir
nvt_*.mdp
npt_*.mdp
md_*.mdp
mdout.mdp
ions.mdp
em.mdp

# Topology include files (generated during pdb2gmx)
posre_*.itp
topol_*.itp

# Custom simulation time files (when simulation_time != 100)
md_*_*.gro
md_*_*.xtc
md_*_*.tpr
!md_final_*.gro
!md_final_*.xtc

# Other intermediate files
hbond.ndx

# Temp directories (keep structure, ignore intermediate contents)
# Note: Global patterns above already handle most intermediate files
# These patterns ensure required files in temp directories are tracked
**/temp/*
!**/temp/.gitkeep
# Required files that should be tracked in temp directories
!**/temp/*.csv
!**/temp/*.pkl
!**/temp/*.np
!**/temp/*.pdb
!**/temp/*.gro
!**/temp/*.top
!**/temp/*.ndx
!**/temp/sconf_*.log
# Required trajectory files (explicit patterns to ensure they're tracked)
!**/temp/md_final_*.xtc
!**/temp/md_final_*.gro
!**/temp/md_*.tpr
!**/temp/processed.gro
# Required descriptor files (xvg files, but exclude intermediate ones)
!**/temp/*.xvg
**/temp/covar_*.xvg
**/temp/charge_*.xvg
**/temp/field_*.xvg

# Run data directories (keep structure, ignore intermediate contents)
**/run_data/**/temp/*
!**/run_data/**/temp/.gitkeep
**/run_data/**/logs/*
!**/run_data/**/logs/.gitkeep

# ===========================================
# DevEx additions
# ===========================================
.uv
__pycache__
.pytest_cache
.coverage
htmlcov
dist
build
*.egg-info
burner_docs/
.DS_Store
*.so
*.dylib
abmelt_infer_pipeline/my_antibody.pdb
